import Navigation from "../Navigation";
import { Header } from "./AppBar.styled";

const AppBar = () => {
  return (
    <Header>
      <p>+++Header+++</p>

      <Navigation />
    </Header>
  );
};

export default AppBar;
